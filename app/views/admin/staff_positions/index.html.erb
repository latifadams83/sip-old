<%= provide(:page_title, "Staff Positions")%>
<%= provide(:staff_active, "w3-theme-d4 w3-round")%>
<%= provide(:staff_positions_active, " w3-black")%>

<table class="w3-table-all ">
  <thead>
    <tr class="w3-theme-d4">
      <th>Position</th>
      <th>Category</th>
      <th>Actions</th>
    </tr>
  </thead>

  <tbody>
    <% @staff_positions.each do |position| %>
      <tr>
        <td><%= position.name %></td>
        <td><%= position.staff_category.name.capitalize %></td>
        <td>
        <%= link_to 'Edit', edit_admin_staff_position_path(position), class: "w3-btn w3-tiny w3-amber"  %>
        <%= link_to 'Delete', admin_staff_position_path(position), method: :delete, data: { confirm: 'Are you sure?' }, class: "w3-btn w3-tiny w3-red"  %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>


<%= link_to '+ Staff Grade', new_admin_staff_position_path, class: "w3-btn w3-tiny w3-theme-d4 w3-right w3-round"  %>
