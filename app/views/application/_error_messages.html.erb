<% if object.errors.any? %>
  <div class="w3-row-padding" id="error_explanation">
    <div class="w3-alert">
      <h6><%= pluralize(object.errors.count, "error") %> prohibited this object from being saved:</h6>

      <ul>
      <% object.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  </div>
<% end %>
