<%= provide(:page_title, "Edit #{@staff.first_name.capitalize+' '+@staff.last_name.capitalize}")%>
<%= provide(:staff_active, "active")%>

<%= render 'form', staff: @staff %>

<%= link_to 'Cancel', staffs_path, class: "w3-btn w3-round w3-left"%>
</div>
</div>
