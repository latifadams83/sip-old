<%= provide(:page_title, "New staff Category")%>
<%= provide(:staff_active, "activelink")%>

<%= render 'form', staff_category: @staff_category, url: staff_categories_url(@staff_category) %>


  <%= link_to 'Cancel', staff_categories_path, class: "w3-btn w3-round w3-left w3-margin-right" %>
  </div>

</div>

</fieldset>
