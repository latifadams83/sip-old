<fieldset class="w3-margin-top ">
  <legend class="w3-label"><b>Editing <%= Level.find(@badge.level_id).lev+@badge.name%></b></legend>

  <div class="w3-round w3-theme-l4">
    <%= render 'form', badge: @badge %>
    <%= link_to 'Cancel', badges_path, class: "w3-btn w3 w3-round w3-left w3-margin-bottom" %>

    </div>

  </div>

</fieldset>
