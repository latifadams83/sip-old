<%= provide(:page_title, "New Staff Position")%>
<%= provide(:staff_active, "activelink")%>

  <%= render 'form', staff_position: @staff_position, url: staff_positions_url %>

  <%= link_to 'Cancel', staff_positions_path, class: "w3-btn w3-round w3-left w3-margin-right" %>
  </div>
