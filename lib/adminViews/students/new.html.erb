<%= provide(:page_title, "New student")%>
<%= provide(:students_active, "activelink")%>

  <%= render 'form', student: @student, url: admin_students_url %>

      <%= link_to 'Cancel', admin_students_path, class: "w3-btn w3-round w3-left w3-margin-top" %>
    </div>
  </div>
