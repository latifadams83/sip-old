<%= form_for(academic_record, url: url) do |f| %>
  <% if academic_record.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(academic_record.errors.count, "error") %> prohibited this academic_record from being saved:</h2>

      <ul>
      <% academic_record.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :type %>
    <%= f.text_field :type %>
  </div>

  <div class="field">
    <%= f.label :cert_earned %>
    <%= f.text_field :cert_earned %>
  </div>

  <div class="field">
    <%= f.label :institution %>
    <%= f.text_field :institution %>
  </div>

  <div class="field">
    <%= f.label :duration %>
    <%= f.text_field :duration %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
